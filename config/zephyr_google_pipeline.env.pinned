# Deprecated: kept only as compatibility pointer.
# Single source of truth for env template is now repository root `.env.example`.
#
# Usage:
#   cp .env.example .env
#   # then fill required secret values
